using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Wall_Grab : MonoBehaviour
{

    /// <summary>
    /// if left fist makes contact with this object 
    /// then pull player towards fist 
    /// 
    /// 
    /// </summary>


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
